# set visible (archived laporan) --done
# user route if admin abort 404

# archive tanggapan
# set visible (archived tanggapan)
# permanent delete (laporan, tanggapan)

# create component button-launch
# create component modal

# create docs where to archive, unarchive, update status
# create sub-title

# find a way to search role (relationship)